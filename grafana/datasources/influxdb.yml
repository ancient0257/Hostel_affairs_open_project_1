apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: iotlab-org
      defaultBucket: sensor-data
    secureJsonData:
      token: iotlab-token-change-me
    editable: true
